# Hilt, Room, Compose, Coroutines, and DataStore all ship their own
# consumer-proguard rules in the AAR, so nothing extra is required for
# this app. Add module-specific keeps here as the project grows.
